# API URL — leave empty for local dev (defaults to http://localhost:8000/api)
# For VPS: set to http://YOUR_VPS_IP:8000/api
VITE_API_URL=

# Google OAuth Client ID (get from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=

# Set to 'true' to use legacy username/password login instead of Google OAuth onboarding
VITE_LEGACY_LOGIN=false
